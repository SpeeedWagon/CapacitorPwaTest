import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deepcode.thermometer',
  appName: 'capexp',
  webDir: 'dist',
  bundledWebRuntime: true
};

export default config;
